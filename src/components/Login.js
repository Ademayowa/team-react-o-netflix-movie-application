import React from 'react';
// import hero from './hero.jpg';

// console.log(hero);

const Login = () => {
  return (
    <div>
      <h1 className="text-center mt-5 bg-light">Login page work in progress</h1>
    </div>
  );
};

export default Login;